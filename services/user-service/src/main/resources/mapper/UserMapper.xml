<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.company.user.mapper.UserMapper">

    <!-- snake_case <-> camelCase 는 공통 설정(MyBatisConfig)에서 자동 처리 -->

    <insert id="insert" parameterType="com.company.user.domain.User"
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO users (login_id, password, name, email, phone, role, created_at, created_by)
        VALUES (#{loginId}, #{password}, #{name}, #{email}, #{phone}, #{role}, #{createdAt}, #{createdBy})
    </insert>

    <update id="updatePassword">
        UPDATE users
           SET password = #{password},
               updated_at = CURRENT_TIMESTAMP,
               updated_by = #{updatedBy}
         WHERE id = #{id}
    </update>

    <select id="findById" resultType="com.company.user.domain.User">
        SELECT * FROM users WHERE id = #{id}
    </select>

    <select id="findByLoginId" resultType="com.company.user.domain.User">
        SELECT * FROM users WHERE login_id = #{loginId}
    </select>

    <select id="findPage" resultType="com.company.user.domain.User">
        SELECT * FROM users
        ORDER BY id DESC
        LIMIT #{page.size} OFFSET #{page.offset}
    </select>

    <select id="countAll" resultType="long">
        SELECT COUNT(*) FROM users
    </select>
</mapper>
