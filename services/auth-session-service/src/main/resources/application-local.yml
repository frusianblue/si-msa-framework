# =====================================================================
# local : 로컬 개발 기본 = 외부 설치 0, DB 없음
#   실행: ./gradlew :services:auth-session-service:bootRun
#
# 보안-영속 결합 분리(2026-06-07) 이후: framework-security 가 mybatis/spring-jdbc 를 전이로 끌어오지 않으므로
# DataSourceAutoConfiguration 이 활성화되지 않는다 → DataSource/H2 설정·드라이버가 전혀 필요 없다.
# (인증만 사용 + dynamic-authorization=false → RBAC DB 조회 없음. 인메모리 인증기로만 동작.)
#
# 별도 오버라이드가 없어 비어 있다(공통 application.yml 만으로 기동). RBAC DB 를 쓰는 실서비스로 전환 시
# framework-security-rbac-mybatis + DB 드라이버 + datasource 설정을 여기/prod 프로파일에 추가한다.
# =====================================================================
