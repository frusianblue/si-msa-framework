# HANDOFF_SECTION7_APPEND_2026-06-07-B.md — `docs/_internal/HANDOFF.md §7(현재 상태/다음 작업)` 말미에 append

- **🟢 B안(호스트 접속 ingress화) 산출 완료 / 🟡 실행 대기 (2026-06-07 세션4, devops)**: standalone kind 호스트 접속을 **port-forward 졸업** → 브라우저 `http://harbor.local`/`http://jenkins.local`. 방식=클러스터 **재생성 + `extraPortMappings`(80/443) + ingress-nginx(`controller-v1.13.0` 핀) + Ingress(harbor/jenkins) + split-horizon 재조정**. 핵심 설계: 세 주체가 같은 이름 `harbor.local` 로 ingress 에 닿되 해소 IP 만 다름 — 호스트=127.0.0.1(OS hosts→localhost:80 extraPortMappings), 인-클러스터 Kaniko=ingress-nginx **ClusterIP**(CoreDNS hosts), 노드 containerd=**CP_IP**(노드 `/etc/hosts`, registry-trust DaemonSet 이 CM `node-etc-hosts` 로 기입). `externalURL=http://harbor.local`(realm 도 이름) → 세 경로 모두 해소. **개정 자산**: `kind-config.yaml`(extraPortMappings+ingress-ready, **extraMounts 제거**=재부팅 휘발 졸업), `01-pull-sanity.sh`(extraMounts 대체로 reg.local certs.d 직접 시드), `registry-trust-daemonset.yaml`(노드 /etc/hosts 멱등병합 추가, inode 보존 위해 `cat>` 덮어쓰기), `08-harbor-install.sh`(ingress 모드+CoreDNS→ClusterIP+노드 /etc/hosts CP_IP), `harbor-values.yaml`(expose.ingress, ssl-redirect=false, proxy-body-size=0), `09-jenkins-install.sh`+`jenkins-values.yaml`(ingress jenkins.local, jenkinsUrl, ClusterIP), `07-reboot-recover.sh`(CP_IP 재산출→CM 갱신→노드 재기입), `certs.d/harbor.local/hosts.toml`(http://harbor.local). **신규**: `10-ingress-nginx.sh`(kind manifest 핀 설치, localhost→404 PASS), `11-host-access-verify.sh`(호스트/노드/인-클러스터 좌표 일괄 PASS 게이트). **신규 함정 8건**: `_PITFALLS_APPEND_2026-06-07-B.md`(ingress만으론 호스트 노출 불가·realm 이름해소 위한 노드 /etc/hosts·ingress-ready 라벨·ssl-redirect/proxy-body-size·CoreDNS ClusterIP·CP_IP 변동 추종·extraMounts 졸업 시 sanity 자기시드·/etc/hosts inode 보존). **저자환경 실행 불가**(Gradle/kind/helm 차단) → 받는 쪽이 런북 순서대로 실행. 받는 쪽 11-host-access-verify PASS 전까지 `NEXT_INGRESS_HOST_ACCESS.md` 는 planning 유지(PASS 후 archive). 문서: `STANDALONE_KIND_HARBOR_JENKINS.md` ingress 런북 전면 개정. **다음 = 받는 쪽 B안 실행 5스테이지 → 11 PASS → 파이프라인 1회 → commit/push.**
